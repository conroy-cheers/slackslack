# Parity scope policy

Still out of scope for the current parity program:
- mobile-only parity
- admin/org-management/owner-only tooling

Allowed by the current parity program:
- experiment-only / flagged endpoints when they are relevant to first-party parity

Live canaries must stay focused on stable, in-scope web/desktop behavior.
